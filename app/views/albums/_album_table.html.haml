%h1 This is from album_table
%h2 ##ALBUM
%table
  %thead
  %tr
    %th action
    %th name
    %th released
    %th released_at
    %th cover_art_url 
    %th kind
    %th length 
  %tbody
  - @albums.each do |album|
    %tr 
      %td
        = link_to 'Edit', edit_album_path(album.id) 
        |
        = link_to 'Delete', album_path(album.id), method: :delete, data: {confirm: "Are you sure"}
      %td= album.name
      %td= album.released
      %td= album.released_at
      %td= album.cover_art_url
      %td= album.kind
      %td= album.length 
%br
= link_to 'New Album', 'albums/new'